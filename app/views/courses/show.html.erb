<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @course.name %>
</p>

<p>
  <strong>Category:</strong>
  <%= link_to @course.category.name, category_path(@course.category_id) %>
</p>





<% if can? :update, @course %>
	<%= link_to 'Edit', edit_course_path(@course) %> |
<% end %>

<%= link_to 'Back', courses_path %>
